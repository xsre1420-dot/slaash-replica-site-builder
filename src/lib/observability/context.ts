/** @deprecated Import from ./correlation — re-export for backward compatibility */
export {
  setObservabilityRoute,
  setObservabilityUser,
  setObservabilityTenant,
  newTrace,
  getCorrelationContext,
  buildEventBase,
  buildCorrelationHeaders,
  newRequestId,
  runWithJobContext,
  resetCorrelationForTests,
  setCorrelationForTests,
  CORRELATION_HEADERS,
} from './correlation';
